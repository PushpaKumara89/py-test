# name: KubePulse CI Pipeline kubepulse-backend

# on:
#   push:
#     branches: [ "main" ] 
#     paths:
#       - 'kubepulse-backend/**'
#   pull_request:
#     branches: [ main ]
#     paths:
#       - 'kubepulse-backend/**'

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout Code
#       uses: actions/checkout@v4

#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     # 💡 Docker Hub එකට Login වීම (Secrets පස්සේ සෙට් කරමු)
#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

        
#     - name: Build and Push Backend
#       uses: docker/build-push-action@v6
#       with:
#         context: ./kubepulse-backend
#         push: true
#         tags: |
#           ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-backend:latest
#           ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-backend:v${{ github.run_number }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max

#     - name: Update Image Tag in K8s Manifest
#       run: |
#         SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
#         sed -i "s|image: ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-backend:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-backend:v${{ github.run_number }}|g" k8s/kubepulse-backend-depl.yaml
      
#     - name: Commit and Push Changes back to Main
#       run: |
#         git config --global user.name "github-actions[bot]"
#         git config --global user.email "github-actions[bot]@users.noreply.github.com"
#         git add k8s/kubepulse-backend-depl.yaml
              
#         SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
#         git commit -m "chore(cd): update kubepulse-backend image to ${{ github.run_number }} [skip ci]" || echo "No changes"  
      
#     - name: Push with Retry
#       uses: nick-fields/retry@v3
#       with:
#         timeout_minutes: 2
#         max_attempts: 5
#         retry_wait_seconds: 5
#         command: |
#           git pull origin main --rebase
#           git push origin main