# name: KubePulse CI Pipeline kubepulse-ui

# on:
#   push:
#     branches: [ "main" ]
#     paths:
#       - 'kubepulse-ui/**'
#   pull_request:
#     branches: [ main ]
#     paths:
#       - 'kubepulse-ui/**'

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout Code
#       uses: actions/checkout@v4

#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}

#     # 🐳 2. Frontend Image එක Build කරලා Push කිරීම
#     - name: Build and Push Frontend
#       uses: docker/build-push-action@v6
#       with:
#         context: ./kubepulse-ui
#         push: true
#         tags: |
#           ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-frontend:latest
#           ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-frontend:v${{ github.run_number }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max
    
#     - name: Update Image Tag in K8s Manifest
#       run: |
#         SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
#         sed -i "s|image: ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-frontend:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/kubepulse-frontend:v${{ github.run_number }}|g" k8s/kubepulse-frontend-depl.yaml

#     - name: Commit and Push Changes back to Main
#       run: |
#         git config --global user.name "github-actions[bot]"
#         git config --global user.email "github-actions[bot]@users.noreply.github.com"
#         git add k8s/kubepulse-frontend.yaml
              
#         SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
#         git commit -m "chore(cd): update kubepulse-frontend image to ${{ github.run_number }} [skip ci]" || echo "No changes"  

#     - name: Push with Retry
#       uses: nick-fields/retry@v3
#       with:
#         timeout_minutes: 2
#         max_attempts: 5
#         retry_wait_seconds: 5
#         command: |
#           git pull origin main --rebase
#           git push origin main