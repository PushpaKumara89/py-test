server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # All requests should go to index.html (for Angular routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
